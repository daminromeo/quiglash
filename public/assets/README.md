Photos and videos attached to questions land here (via the /edit page).

These files are committed to the repo on purpose: hosted free tiers wipe the
filesystem when the app restarts, so anything not committed would disappear.
